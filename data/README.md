# Данные

- `raw/` — сырые скачанные датасеты (в git **не коммитятся**)
- `processed/` — обработанные данные для загрузки в БД (в git **не коммитятся**)

Как получить каждый датасет — скрипты в `scripts/`, описание — в `docs/data/`.
