# Datasets — describe sources in docs/data/, don't commit binaries
# (исключаем содержимое, а не папку: иначе git игнорирует и «!»-исключения ниже)
data/raw/*
data/processed/
*.tif
*.tiff
*.nc
*.parquet

# Node / Vue
node_modules/
dist/
.vite/
*.local

# .NET
bin/
obj/
*.user
.vs/

# Python / ML
__pycache__/
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/

# Env & secrets
.env
.env.*
!.env.example
appsettings.*.local.json

# OS / IDE
.DS_Store
.idea/
!data/raw/labels_*.csv
# Малые производные CSV пилота — коммитим для воспроизводимости демо (make demo-data)
!data/raw/settlements_*.csv
!data/processed/
!data/processed/features_*.csv
!data/processed/scores_*.csv
# Зимний ML: датасет и модель — чтобы train/seasons воспроизводились без выкачивания
# (сырые кэши data/raw/storm_archive/ и data/raw/winter_daily/ не коммитим)
!data/raw/winter_storm_warnings.csv
!data/processed/winter_daily_features.csv
!data/processed/winter_lgbm.txt

# загрузки страницы «Данные» (локальный запуск API)
backend/src/Api/data/uploads/
