# Скрипты

Вспомогательные скрипты: скачивание датасетов (Sentinel, FIRMS, ERA5), загрузка в PostgreSQL, генерация отчётов. Один скрипт — одна задача, имя вида `download_firms.py` / `load_scores.py`.
